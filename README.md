# P1 Book Mockup — Core

Standalone 3D book mockup tool. Clean, zero-dependency on external routing.

## Quick Start

```bash
npm install
npm run dev
```

## Build

```bash
npm run build    # outputs to dist/
npm run preview  # preview production build
```
